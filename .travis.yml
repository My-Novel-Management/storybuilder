language: python

python:
    - 3.6

install:
    - pip install coveralls
    - pip install mecab-python3

script:
    - coverage run --rcfile=.coveragerc setup.py test

after_success:
    - coveralls
